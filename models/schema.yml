version: 2

sources:
  - name: gz_raw_data
    description: raw data
    loader: bigquery
    schema: gz_raw_data 
    tables:
      - name: sales
        identifier: raw_gz_sales
      - name: product 
        identifier: raw_gz_product 
      - name: ship
        identifier: aw_gz_ship
